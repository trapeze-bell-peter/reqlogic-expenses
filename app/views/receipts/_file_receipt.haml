%h3{class: expense_entry.sequence>1 && 'page-break'}  Receipt for #{expense_entry.sequence}: #{expense_entry.description}

- receipt = expense_entry.receipt

- if receipt.source_file.attachment&.image?
  = image_tag(receipt.source_file, class: expense_entry.receipt.receipt_size.to_s.parameterize)
- else
  - receipt.attachments.to_a.keep_if(&:image?).each do |attachment|
    = image_tag(attachment, class: expense_entry.receipt.receipt_size.to_s.parameterize)