<%= form_with(model: expense_claim, local: true) do |form| %>
  <% if expense_claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense_claim.errors.count, "error") %> prohibited this expense_claim from being saved:</h2>

      <ul>
      <% expense_claim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="form-group row">
    <%= form.label :claim_date, class:  "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= form.date_field :claim_date %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
